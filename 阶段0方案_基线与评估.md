# 阶段0 技术实施方案：环境 + 基线 + 评估体系

> 已裁决事项：LLM 用 DeepSeek（基线/模拟器）+ DashScope 备用；测试数据用合成；纯 Python 状态机；SOP 由本方案给出初版待审核。

---

## 一、SOP 初版设计（待审核）

覆盖四类售后场景：**退款、退货、换货、物流异常**。有向图：节点 = SOP 状态，边 = 合法动作。

### 1.1 状态节点（12 个）

| ID | 节点 | 说明 | 终止? |
|----|------|------|-------|
| S0 | greeting | 接待开场 | |
| S1 | issue_identification | 识别问题类型（退款/退货/换货/物流） | |
| S2 | order_verification | 核实订单信息 | |
| S3 | refund_eligibility | 退款资格核验（未发货可直接退；已发货需走退货） | |
| S4 | evidence_collection | 证据收集（破损/质量问题需照片） | |
| S5 | evidence_review | 证据核验 | |
| S6 | logistics_check | 物流状态查询与异常判断 | |
| S7 | solution_proposal | 方案提议（退款/补发/换货/赔付券，金额受限） | |
| S8 | user_confirmation | 等待用户确认方案 | |
| S9 | execution | 执行操作（发起退款/生成退货单/补发） | |
| S10 | resolution | 已解决，礼貌收尾 | ✅ |
| S11 | escalation | 转人工/升级 | ✅ |

### 1.2 动作集合（15 个）与转移

| 动作 | 从 → 到 | 说明 |
|------|---------|------|
| greet_and_ask | S0 → S1 | 问候并询问问题 |
| clarify_issue | S1 → S1 | 追问问题细节（自环，限 2 次） |
| ask_order_info | S1 → S2 | 索要订单号 |
| query_order | S2 → S3/S4/S6 | 查订单，按问题类型分流：退款→S3，退货换货→S4，物流→S6 |
| check_refund_policy | S3 → S7 | 核验退款资格后给方案 |
| request_evidence | S4 → S5 | 请用户上传照片/描述 |
| review_evidence | S5 → S7/S11 | 证据有效→方案；证据不足且用户拒绝补充→升级 |
| query_logistics | S6 → S7 | 查物流后给方案 |
| propose_solution | S7 → S8 | 提出具体方案 |
| revise_solution | S8 → S7 | 用户不满意，换方案（限 2 次） |
| execute_action | S8 → S9 | 用户同意后执行 |
| confirm_resolution | S9 → S10 | 确认完成 |
| comfort_user | 任意非终止节点自环 | 安抚情绪（不推进流程） |
| escalate | 任意非终止节点 → S11 | 情绪激化/超权限/证据纠纷时升级 |
| end_session | S10/S11 | 会话结束标记 |

### 1.3 硬性合规规则（违反即记违规）

1. 未到 S9 不得执行任何操作类动作（execute_action）
2. 未核实订单（未经过 S2）不得给出方案
3. 退货/换货必须经过证据核验（S4→S5）
4. 承诺金额上限：赔付券 ≤ 订单金额 20%，超出必须 escalate
5. 动作必须在当前节点的合法动作集合内（action mask）

> **审核点 A**：节点/动作粒度是否合适？合规规则 4 的 20% 上限是拍的，可调。

---

## 二、系统模块（输入 / 输出 / 测试用例）

### M1 LLM 客户端 `src/llm.py`
- **输入**：messages 列表、模型名、温度
- **输出**:文本 或 JSON（结构化动作）
- **测试**：① 黑箱探测：发最小请求确认连通与返回格式；② JSON 模式输出可解析；③ 错误 key / 网络失败时抛出明确异常
- API key 通过项目根目录 `.env` 读取（`DEEPSEEK_API_KEY`，阶段2 加 `DASHSCOPE_API_KEY`），`.env` 不入库

### M2 SOP 引擎 `src/sop.py` + `sop/sop_graph.json`
- **输入**：当前状态 ID、动作名
- **输出**：合法动作集合（action mask）、转移后状态、是否违规、是否终止
- **测试**：① 正常转移路径（4 类场景各走通一遍）；② 非法动作被拒并记违规；③ 自环次数上限触发；④ 终止节点后不再接受动作

### M3 测试集生成 `scripts/gen_test_cases.py` → `data/test_cases.json`
- **输入**：场景模板（退款/退货/换货/物流 4 类 × 情绪档位 3 档 × 若干商品情境），调 DeepSeek 生成
- **输出**：60 条测试用例，每条含：`case_id、场景类型、用户人设（情绪/耐心值）、订单信息（金额/发货状态）、首句客诉、期望终点（resolution 或 escalation）`
- **测试**：① 60 条 schema 校验全通过；② 4 类场景分布均衡；③ 人工抽查 5 条自然度（贴我给你看）

### M4 用户模拟器 `src/user_simulator.py`
- **输入**：测试用例（人设+订单+首句）、对话历史
- **输出**：下一句用户话语 + 满意度信号（satisfied / neutral / angry）
- **测试**：① 正常：对合理方案表示接受；② 边界：耐心值低的人设在被反复追问后升级情绪；③ 错误：Agent 答非所问时模拟器表达不满而非配合

### M5 基线 Agent `src/baseline_agent.py`
- 实现：单次 LLM 调用，prompt = SOP 全文规则 + 对话历史，**结构化输出** `{action: str, reply: str}`（动作用于合规检查，回复发给用户）
- **输入**：对话历史、SOP 规则文本
- **输出**：`{action, reply}`
- **测试**：① 首轮输出 greet_and_ask；② 输出动作名在动作全集内（格式层面）；③ 输出 JSON 可解析率 ≥ 95%（20 次采样）

### M6 对话运行器 `src/runner.py`
- **输入**：测试用例 + Agent + 模拟器，最大轮数 12
- **输出**：完整轨迹 JSON（每轮：状态、动作、是否违规、用户回复、满意度）
- **测试**：① 单条对话端到端跑通并落盘；② 超轮数强制截断标记为未完成；③ LLM 调用失败时轨迹标记 error 而非崩溃

### M7 评估 `src/evaluate.py`
- **输入**：轨迹目录
- **输出**：报告（SOP 合规率 = 合规动作数/总动作数、任务完成率 = 到达终止节点比例、平均对话轮数），CSV + 控制台表格
- **测试**：用 3 条手工构造的轨迹（全合规/含违规/未完成）验证指标计算正确

---

## 三、交付顺序与验收（逐级闭环）

```
步骤1  环境搭建 + M1 黑箱探测          验收：DeepSeek 最小请求返回正常
步骤2  M2 SOP 引擎 + 单元测试          验收：pytest 全绿（含扰动用例）
步骤3  M3 测试集生成                   验收：60 条通过 schema 校验，人工抽查 5 条
步骤4  M4 模拟器 + M5 基线（各自单测）  验收：各模块独立测试通过
步骤5  M6 运行器，先跑 3 条对话联调     验收：轨迹完整、人工读一遍合理
步骤6  全量 60 条 + M7 评估            验收：产出基线三指标数字 ← 阶段0 里程碑
```

预计 LLM 调用量：约 60 条 × 平均 8 轮 × 2 次调用（Agent+模拟器）≈ 1000 次 DeepSeek 调用，成本约 1~3 元。

## 四、目录结构

```
电商智能体框架/
├── sop/sop_graph.json
├── src/{llm,sop,user_simulator,baseline_agent,runner,evaluate}.py
├── scripts/gen_test_cases.py
├── data/test_cases.json
├── runs/                # 轨迹与评估输出
├── tests/               # pytest
├── .env                 # API keys（你来填）
└── requirements.txt     # openai(兼容deepseek), python-dotenv, pytest
```

---

## 阶段0 结果（2026-07-09，60 条全量，0 错误）

| 范围 | 严格合规率 | 重同步合规率 | 完成率 | 平均轮数 | 终点符合率 |
|------|-----------|------------|--------|---------|-----------|
| overall | 25.1% | 67.4% | 100% | 6.45 | 95.0% |
| refund | 28.4% | 56.8% | 100% | 6.33 | 86.7% |
| return | 19.8% | 63.2% | 100% | 7.07 | 93.3% |
| exchange | 26.2% | 74.8% | 100% | 6.87 | 100% |
| logistics | 26.5% | 75.9% | 100% | 5.53 | 100% |

- 评估双口径（用户裁决）：严格=违规不推进状态；重同步=违规也跳转、每步独立计量
- 终点不符 3 条（refund_08/12、return_13）：期望 resolution 但被升级到 S11，最后满意度均为 angry——基线安抚失败误升级
- 基线典型违规模式：用户主动提供信息后跳过 SOP 步骤（如跳过 ask_order_info 直接 query_order）
- 轨迹与指标：`runs/baseline/`，重跑命令 `python src/runner.py --out runs/baseline --workers 6 && python src/evaluate.py runs/baseline`

## 待你确认的点

- **A**：SOP 初版（一、1.1~1.3）是否通过？尤其是合规规则 4 的金额上限
- **B**：测试集规模定 60 条（4 场景 × 15 条）是否可以
- **C**：确认后我从步骤1 开始，你需要先在 `.env` 里填入 `DEEPSEEK_API_KEY`
