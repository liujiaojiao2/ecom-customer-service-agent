# 电商售后 MCTS Agent 实现计划

> 原则：先做能跑通的弱版本（MVP），再逐步强化。每个阶段结束时有可演示的产出。

---

## 阶段0：环境 + 基线（1周）

**目标**：有一个可以对比的基线，整个后续实验有了参照系。

**具体任务**：
- 搭好代码框架（建议 Python + LangGraph or 纯 Python state machine）
- 用 JDDC 数据集抽取 50~100 条售后对话作为测试集
- 实现基线：LLM（Qwen/DeepSeek）+ SOP规则提示词，直接生成回复
- 定义评估指标（先用可自动计算的代理指标）：
  - **SOP合规率**：动作是否在当前SOP节点允许的范围内（规则检查，不用模型）
  - **对话轮数**：越少越好
  - **任务完成率**：是否到达SOP终止节点（resolution/escalation）

**产出**：基线跑通，有数字，后续所有实验都与这个数字对比。

---

## 阶段1：SOP建模 + MCTS核心（2~3周）

**目标**：MCTS能在SOP约束下搜索出一条合理动作序列。

**具体任务**：

### SOP建模
- 将售后SOP流程建成有向图（节点=状态，边=合法动作）
- 每个节点定义：允许的动作集合（action mask）、是否为终止节点
- 建议用 JSON 或 Python dataclass 表示，不要过度工程化

### MCTS核心（弱版本）
- 实现标准 UCT（不是 PUCT，先不用策略网络）
- **Policy prior**：uniform（所有合法动作等概率）或 LLM zero-shot 打分
- **Value estimation**：rollout 模拟（用用户模拟器跑到底，取奖励均值）
- **奖励函数**（参考上方设计）：terminal reward 为主

### 用户模拟器（轻量版）
- 用 LLM + 固定人设 prompt 模拟用户（参考 JDDC 真实对话的用户说法）
- 能回答 Agent 的反问，能表达情绪（满意/不满）
- 先不追求高保真，能让 MCTS rollout 跑完一轮即可

**产出**：MCTS 能在 10 条测试对话上比基线降低 SOP 违规率，或减少对话轮数。有数字就是里程碑。

---

## 阶段2：多模态状态识别（2周）

**目标**：能从图文+行为输入中准确定位当前 SOP 状态节点。

**具体任务**：
- 先用 **Qwen-VL few-shot prompt** 做状态分类，测试准确率
- 如果准确率 > 85%：直接用，不训练编码器
- 如果准确率 < 85%：考虑用合成数据微调一个轻量分类头（冻结 Qwen-VL backbone）
- 行为流语义化：将用户行为序列转为自然语言描述（规则模板即可，不用模型）

**决策点**：如果 zero-shot 已经够好，跳过编码器训练，直接进阶段3。这是一个实验结果驱动的决策，不是提前定好的。

**产出**：状态识别模块接入 MCTS，端到端可以跑一条多模态输入的对话。

---

## 阶段3：数据合成 + 策略/价值网络（可选，3~4周）

> ⚠️ 这个阶段视时间和算力决定是否做。如果跳过，MCTS 用 LLM 作为 policy prior 也能 work，策略/价值网络作为 ablation 的"有无对比"存在于论文里即可。

**数据合成**：
- 从 JDDC 抽取首句 → Qwen-VL-Max 逆向推导情绪+行为 → 双智能体自对弈
- 目标合成量：5000~10000 条轨迹（M3 本地跑太慢，建议用云 API 批量生成）
- 每条轨迹需要记录：状态序列、动作序列、奖励、是否合规

**策略网络训练**：
- 输入：SOP 当前状态 embedding
- 输出：合法动作的概率分布
- 用合成轨迹做监督学习（行为克隆），再用 MCTS 自对弈数据迭代

**价值网络训练**：
- 输入：对话状态
- 输出：预测最终累计奖励
- 替代 rollout，加速 MCTS 搜索

**产出**：有策略/价值网络的 MCTS（强版本）vs. 无网络的 MCTS（弱版本）的 ablation 实验。

---

## 阶段4：实验 + 论文（2~3周）

**评估实验**：

| 实验 | 目的 |
|------|------|
| 基线 vs. MCTS（弱版本） | 证明框架有效 |
| MCTS（弱版本）vs. MCTS（强版本） | 证明策略/价值网络的增益 |
| 有SOP masking vs. 无masking | 证明合规约束的必要性 |
| 奖励权重敏感性实验 | 证明奖励设计的鲁棒性 |

**核心 claim 的数字支撑**：
- SOP 合规率：基线 vs. MCTS
- 任务完成率：基线 vs. MCTS
- 平均对话轮数：基线 vs. MCTS

---

## 优先级总结

```
必须做（MVP）：
  阶段0（基线）+ 阶段1（MCTS核心）→ 这两个做完论文就有东西可写

建议做：
  阶段2（多模态）→ 体现你的背景（之前做过多模态客服）

视时间决定：
  阶段3（策略/价值网络）→ 如果秋招压力大，这个可以简化或作为 future work
```

---

## 当前最紧迫的下一步

1. **明天就能做**：把 SOP 流程画成状态图（纸上或 draw.io），确认节点和动作集合
2. **本周内**：跑通基线，得到第一个合规率数字
3. **两周内**：MCTS 最简版本能跑一条对话

**不要因为策略网络和价值网络还没想清楚而卡住——先让 MCTS 跑起来，其他是锦上添花。**
