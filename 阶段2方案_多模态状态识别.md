# 阶段2 技术实施方案：多模态状态识别

> 目标：给定用户首轮输入（**文本首句 + 图片 + 近期行为流**），准确定位当前 SOP 状态节点，接入 MCTS 作为 branch_ctx（`issue_type` 与 `evidence_valid`）。
> 里程碑：分类准确率 > 85%，端到端多模态对话可以跑通至少 1 条。

---

## 一、任务定义

阶段0/1 里 `issue_type` 是从测试用例元数据直接读取的（作弊）。阶段2 让感知层从**多模态输入**推断出来。

**输入**：
```
{
  "text": "衣服破了一角，收到就这样",           # 用户首句
  "image": "path/to/broken_package.jpg",       # 商品/破损/物流照片
  "behaviors": ["查看订单详情", "点击'申请售后'", "上传照片"]  # 近期行为流
}
```

**输出**：
```
{
  "issue_type": "return",           # refund/return/exchange/logistics，4分类
  "has_evidence": true,             # 图片是否为有效证据
  "confidence": 0.92
}
```

MCTS 用这两个字段填 `branch_ctx`：`{"issue_type": ..., "evidence_valid": has_evidence}`。

## 二、方法（弱→强递进）

**先做 zero-shot / few-shot Qwen-VL**（对齐原计划）：
1. Zero-shot：直接 prompt 让 Qwen-VL 输出分类结果 + 置信度
2. 若准确率 <85%，加 5~8 shot 例子（图 + 分类）
3. 若仍 <85%，才考虑训练轻量分类头（阶段2 后半程再议）

## 三、模块

### M13 DashScope 客户端 `src/llm_vl.py`
- 输入：文本 prompt + 本地图片路径列表 + 温度
- 输出：文本或 JSON
- 复用阶段0 的 `_extract_json`
- 测试：黑箱探测 3 档——① 纯文本 chat；② 单图 + 文本；③ 错误 key/损坏图片抛异常

### M14 多模态测试集 `data/mm_test_cases/`
详见裁决 D5/D6。每条样本含：图片文件、文本、行为流、金标签（issue_type + has_evidence）。

### M15 感知层 `src/perception.py`
- 输入：dict{text, image, behaviors}
- 输出：dict{issue_type, has_evidence, confidence}
- Zero-shot prompt 模板 + JSON 抽取
- 测试：正常 4 类各 1 条给出正确分类；边界（图不清、行为流冲突）不崩溃；错误（图片缺失）明确报错

### M16 评估 `src/mm_eval.py`
- 输入：测试集目录
- 输出：准确率、per-class 精确率/召回率、混淆矩阵、每错例的详情
- 测试：手工 3 条轨迹验证指标正确

### M17 端到端接入 `src/mcts_agent.py` 增强
- MCTSAgent 增加 `perceive(mm_input)` 初始化路径：调 perception → 填 branch_ctx → 常规 MCTS
- 端到端联调：1 条多模态输入的完整对话，全程零违规

## 四、交付顺序

```
步骤12  DashScope 客户端 + 黑箱探测          （0.5 天）
步骤13  多模态测试集构造 20 条              （1~2 天，依裁决 D5/D6）
步骤14  感知层 zero-shot + 4 类各 1 条冒烟   （0.5 天）
步骤15  全量准确率评估 + 混淆矩阵            （0.5 天）← 阶段2 里程碑
步骤16  MCTS 端到端接入 + 联调 1 条           （0.5 天）
```

## 阶段2 结果（2026-07-09，20 条全量）

| 指标 | 数值 |
|------|------|
| **issue_type 准确率** | **95.0%** (19/20，超 85% 目标 +10pp) |
| **has_evidence 准确率** | **100.0%** |
| **联合准确率** | **95.0%** |
| API 错误 | 0 |

Per-class：exchange/return F1=100%，logistics F1=90.9%，refund F1=88.9%。
唯一错例 refund_08（"面包怎么还没发"→"没发"文本歧义，被判 logistics）。

**决策点触发**：zero-shot 95% >> 85%，**跳过编码器训练**（原计划 <85% 才训），直接接 MCTS。

**多模态端到端**（return_02）：
```
Qwen-VL 感知（坚果潮湿实物照）→ {issue_type: return, has_evidence: True}
                        ↓
MCTS branch_ctx → 9 轮到达 S10 → 严格违规 0
动作序列：greet → ask_order → query_order → comfort → request_evidence
        → review_evidence → propose → execute → confirm
```

`request_evidence → review_evidence` 路径只有当 `evidence_valid=True` 时才打开，证明感知层输出正确驱动 MCTS 分支。

## 五、成本估算（依赖裁决点）

以推荐配置（20 条测试集、Qwen-VL-Plus、zero-shot）：
- 数据生成：图片生成成本视 D5 定；若用户手工提供图片 = 0 ¥
- 分类推理：20 张图 × Qwen-VL-Plus ≈ ¥2~5
- 端到端联调：1 条对话 ≈ ¥1
