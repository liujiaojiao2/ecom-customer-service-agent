# 阶段1 技术实施方案：SOP 约束下的 MCTS 核心（弱版本）

> 前置：SOP 引擎（12节点/15动作/双口径）、用户模拟器、评估体系已在阶段0交付并验收。
> 里程碑：MCTS 在 10~12 条测试对话上，严格合规率显著高于基线 25.1%（掩码构造上应达 100%），且平均轮数不劣于基线 6.45。

---

## 一、问题建模

每个**真实对话轮**，Agent 调用一次 MCTS 搜索决定动作，再由 LLM 将动作渲染成自然语言回复：

```
真实轮 t:  MCTS搜索(当前状态) → 最优动作 a* → LLM渲染回复 → 用户模拟器回应 → 进入轮 t+1
```

- **搜索状态**：SOP 节点 + 对话历史 + 计数器（自环上限）
- **动作空间**：`engine.legal_actions(state)` —— SOP 硬掩码，违规动作根本不进树
- **随机性**：用户回应是随机转移；采用**开环 MCTS**（树按动作序列索引，用户回应每次模拟重新采样）——弱版本标准做法
- **UCT**：`Q + c·√(ln N / n)`，先验 uniform（不用 PUCT，策略网络留给阶段3）

## 二、奖励函数（terminal reward 为主）

```
R = w1·任务完成 + w2·用户满意 − w3·轮数成本
```

| 项 | 取值 | 默认权重 |
|----|------|---------|
| 任务完成 | S10=1.0；S11=0.4（升级是合法出口但非最优）；未终止=0 | w1=1.0 |
| 用户满意 | 终局满意度 satisfied=+1 / neutral=0 / angry=−1 | w2=0.3 |
| 轮数成本 | 每轮 −1（从搜索起点起算） | w3=0.05 |

合规项不进奖励：掩码已硬性保证搜索内 100% 合规（无掩码消融时才把违规计负奖励，阶段4 再加开关）。
权重敏感性是阶段4的实验，这里只定默认值。

## 三、模块（输入/输出/测试用例）

### M8 奖励函数 `src/reward.py`
- 输入：终局 SOP 节点、终局满意度、轮数；输出：标量奖励
- 测试（纯函数，无 LLM）：S10+satisfied 短对话 > S10+neutral 长对话 > S11 > 未终止；权重可配

### M9 MCTS 核心 `src/mcts.py`
- 输入:根状态、环境接口（step/rollout）、模拟预算 N、UCT 常数 c、最大深度
- 输出：最优动作 + 搜索统计（各动作访问数/Q值）
- **测试用确定性 mock 环境（无 LLM、可控转移）**：
  - ① 已知最优路径的玩具 MDP，N 足够时 MCTS 选出最优动作
  - ② 非法动作从不出现在树中（掩码验证）
  - ③ 反向传播后访问数守恒、Q 值等于手算值
  - ④ 预算 N=1 边界不崩溃

### M10 环境接口 `src/mcts_env.py`
- 把 SOP 引擎 + 用户模拟器 + 奖励函数封装成 MCTS 可用的模拟环境（rollout 到终止或深度上限）
- rollout 内用户回应的保真度 → **裁决点 D1**
- 测试：给定固定动作序列，rollout 返回结构完整的 (轨迹, 奖励)；深度上限截断正确

### M11 MCTS Agent `src/mcts_agent.py`
- 与 BaselineAgent 同接口 `act(history)`，内部：MCTS 选动作 → LLM 按动作生成回复（动作硬编码进 prompt，回复只做渲染）
- 测试：单条测试用例端到端跑通，全程动作零违规

### M12 对比实验脚本
- runner 复用（`--agent mcts` 开关），跑测试子集，evaluate 复用
- 产出：MCTS vs 基线 对比表 ← **阶段1里程碑**

## 四、交付顺序

```
步骤7   M8 奖励函数 + 单测（纯函数，先行）
步骤8   M9 MCTS核心 + mock环境单测（不花一分钱API）
步骤9   M10 环境接口 + 小规模rollout联调（真实LLM，1条对话）
步骤10  M11 MCTS Agent + 单条端到端
步骤11  M12 对比实验（测试子集），产出对比表
```

## 裁决记录（2026-07-09 用户确认）

D1=动作级 rollout；D2=N=16（深度上限4）；D3=默认权重通过；D4=分层 12 条（每场景3条：calm/impatient/angry 各1，含1条过度索赔）。

**修订1（2026-07-09 联调发现视界问题）**：深度上限 4 < S0→S10 最短路径 7 步，rollout 够不到解决奖励，导致 MCTS 首轮理性地选 escalate（稳拿 0.35）。用户裁决：深度上限改为 **8**，纯终局奖励不加人工塑形。

**修订2（同日，rollout 稀疏奖励问题）**：均匀随机 rollout 连续 6+ 步命中唯一正确动作的概率 ≈ 1/1800，深度 8 下仍选 escalate。修复：rollout 默认策略以 0.8 概率偏向推进动作（排除 comfort/clarify/revise/escalate），即领域知情 default policy（AlphaGo fast rollout 同理），树内 UCT 探索不受影响。修复后 greet_and_ask Q=0.457 > escalate 0.35。

## 阶段1 结果（2026-07-09，分层 12 条）

| 指标 | 基线 | MCTS | 差 |
|------|------|------|------|
| **严格 SOP 合规率** | 29.2% | **100.0%** | **+70.8pp** |
| 重同步合规率 | 69.4% | 100.0% | +30.6pp |
| 任务完成率 | 100% | 100% | = |
| 平均对话轮数 | 6.00 | 6.25 | +0.25 |
| 终点符合率 | 100% | 66.7% | -33.3pp |

**核心 claim 数字**：SOP 合规率 29% → 100%，任务完成率持平，轮数几乎持平。里程碑达成。

**终点符合率下降的 4 条错配**（不影响合规率/完成率的对比结论）：
- exchange_01/06、logistics_06（都是 calm/impatient 但被过早 escalate，MCTS 谨慎倾向的副作用）
- logistics_14（angry 过度索赔期望 escalate，模拟器在推进路径下没坚决 escalate → 模拟器保真度问题，属于 future work 分析）

这两类错配都可以在论文里作为"MCTS 在保守/激进之间的 tradeoff 定性讨论"，或用阶段3策略网络的先验做进一步缓解。

## 五、成本估算（依赖裁决点）

以推荐配置（动作级 rollout、N=16、深度上限 4、12 条测试对话）：
每步搜索 ≈ 16×4=64 次模拟器调用 → 每条对话 ~6 步 ≈ 400 次 → 12 条 ≈ 5000 次 DeepSeek 调用 ≈ ¥3~6，并发跑约 30~60 分钟。
全保真 rollout（回复也用 LLM 生成）成本与时间 ×2。
