# PRM 技术实施方案：过程奖励模型（修订版 v3）

> 目标：把稀疏终局奖励（S10/S11 才有信号）增强为步骤级信号，本阶段验证
> 方法链可行性；正式版本等 JDDC 数据 + 服务器资源到位后重训。
> 基线：PUCT + 本地 rollout，终点符合率 86.2%（runs/mcts_local_60）。
> 修订历史：v1 规则标签方案（废弃）→ v2 结果驱动 + 路线A/B → v3 树统计
> 存量确认 + 止损点后移 + 墙钟预算口径。

---

## 一、核心设计决策

### 1.1 标签必须结果驱动，不能规则驱动

~~v1 方案：`if 违规 then -0.5 else ...`~~ **废弃**。规则标签是特征的确定
性函数，训练出的模型等价于把规则蒸馏一遍，零信息增益。

**替代（按优先级）**：

1. **MCTS 树统计（首选）**：节点 Q(s,a) 本身就是"从该状态出发的成功率
   估计"，导出 (状态特征, Q值) 对做回归目标，只取 visits ≥ 阈值的节点。
2. **Qwen rollout 成功率（软标签）**：状态的本地 7B rollout 到达 S10
   比例。本质是把贵的模拟器蒸馏成便宜打分器，与 1 可合并。

这样 clarify/escalate 的价值不再被一刀切：若数据显示 S4 证据不足时回退
S2 的轨迹成功率更高，PRM 自然学到正分——与 SOP 图回退边设计意图一致。

### 1.2 树统计存量确认（Step 0，已完成）

现有 60 条轨迹只保存了**根节点子动作**统计（search_stats: visits+q），
全树节点级统计未落盘。且 budget=16 下典型分布是被选动作 11~12 次、其余
1~4 次——按 N≥5 过滤后**数据集严重偏向被选中的好动作**，负例 Q 值噪声
最大。结论：**必须重跑落盘（Step 2.5）**，不能直接用存量。

### 1.3 特征设计

- SOP 节点 one-hot(12)、动作 one-hot(15)、情绪(3)、满意度(3)
- **时序特征**：当前轮数、距上次状态推进的轮数（学"卡太久"模式）
- 特征与标签不重合：标签是结果驱动 Q 值，特征只是输入信息

### 1.4 模型：小容量 + 可解读 + 容量配置化

- 34→16→1 + dropout，或逻辑/线性回归；容量写成配置项（JDDC 阶段放大）
- 小模型系数可解读，可直接写进论文（如"负面情绪×S4 交互项系数为正"）

### 1.5 数据拆分：必须按轨迹拆

相邻步骤高度相关，按样本随机拆会虚高。留出 **12 条**（每场景 3 条分层）
完全不参与训练作最终评估集；其余 48 条做轨迹级 5 折交叉验证。

---

## 二、两条集成路线（分开实现、分开消融）

~~"每步 PRM 替代终局奖励"~~ **废弃**：总奖励与轨迹长度耦合，可被 exploit。

### 路线 A：PRM 作叶节点价值估计（替代 rollout）

- 终局奖励不动，叶评估从"7B 模拟若干轮"换成"MLP 前向一次"
- 秒级 → 微秒级，同预算 simulation 次数提升几个数量级
- **速度收益是结构性的，与数据量无关**；精度损失报 trade-off
- 支撑论文"生产可用性"叙事

### 路线 B：PBRS 稠密塑形

保留终局奖励，每步附加 `γ·Φ(s′) − Φ(s)`。三个硬约束：
1. Φ 只依赖状态不依赖动作；2. 终止状态 Φ≡0；3. γ 与 MCTS 回传一致。
Telescoping 保证轨迹排序不变（Ng et al. 1999），收益在有限预算下体现。

两路线正交可叠加，但论文必须分开报，否则收益无法归因。

---

## 三、实验阶梯（v3 修订：止损点在 Step 2 之后）

```
Step 0    树统计存量确认                          已完成（需重跑）
Step 1    手写稠密奖励直接接 MCTS（不训练）        0.5天  留出集冒烟
Step 2    手写 Φ 表改 PBRS 形式                   0.5天
          ★ 止损决策点（三档判据，见下）
Step 2.5  重跑 60 条落盘全树统计                  1天（机器时间为主）
          本地 7B rollout + budget≥64 + 记录每次 rollout 耗时
          = 树统计标签 + JDDC 算力测算 + 高预算参照轨迹，一跑三产出
Step 3    结果驱动标签 + 训练小 PRM               1.5天
          含：导出格式设计、过滤阈值调试、Q 分布偏斜检查（偏斜则变换）、
          轨迹级拆分脚本
Step 4    路线 B：学习型 PRM 替换手工 Φ           0.5天
Step 5    路线 A：PRM 替代 rollout                1天
          固定墙钟为主（每步如 5 秒，各自能跑多少次 simulation 本身是
          结果）；固定次数为辅（基线只跑低档，PRM 版跑两档）
Step 6    留出集评估 + 消融表 + bootstrap         1天
          砍法：朴素累加若 Step 2 确认差于 PBRS，消融表只报单预算档
```

**Step 2 后的三档止损判据**（12 条留出集上噪声大，不硬切）：
- 明显提升（≥5pp）→ 继续
- 明显变差 → 先查集成 bug（奖励尺度不匹配、回传重复计算），不是机制问题
- 中间地带 → Step 1 平庸可能是朴素累加长度耦合的锅，看 Step 2 PBRS 修正
  后的真实水平再决策

**总计 6~7 天**。云 API 费用集中在留出集评估对话（真实轮的 DeepSeek
模拟器+渲染，~¥15-20）；训练与 rollout 全本地 ¥0。**注意：留出集评估的
云费用到 JDDC 阶段会等比放大，需在二阶段方案中显式预算。**

论文对比链：终局 only → 规则稠密 → 规则 PBRS → 学习型 PRM 塑形 →
PRM 替代 rollout，每层增量可归因，消融表现成。

---

## 四、评估协议（本阶段固化，两阶段数字才可比）

- 评估**只在留出 12 条**上做
- 预算口径：**固定墙钟时间为主**（生产叙事贴切），固定 simulation 次数
  为辅，两种口径脚本都留
- 指标：终点符合率、平均轮数、**单次决策延迟**、**违规率**（确认塑形
  没有诱导跳过必要步骤）
- bootstrap 置信区间，计算脚本固化
- 结果目录：`runs/prm_step1_dense/`、`runs/prm_step2_pbrs/`、
  `runs/prm_step4_learned/`、`runs/prm_step5_leaf/`，跑完立即 evaluate

---

## 五、两阶段边界：60 条能验证什么 / 不能验证什么

### 能可靠验证（本阶段产出）

1. **工程链路正确性**：导出→标签→训练→集成→评估全链跑通，接口定型。
   JDDC 到手只换数据源。**这是第一阶段的核心产出。**
2. **机制层定性信号**：Step 1/2 不依赖训练数据量，结论可迁移。
3. **路线 A 延迟收益**：加速比结构性成立（精度会变，速度不会）。

### 不能下结论（别过度解读）

- **学习型 PRM 的精度与泛化**：420~1000 样本、12 条留出，差几个点在
  噪声范围内。PRM 没打过硬编码规则 → 只能得出"这个量级学不出来"；
  打平或略超 → 正信号但论文别下重注。

### 为 JDDC 阶段现在就做的解耦设计

1. **标签生成解耦**：`trajectory → List[(features, label)]` 接口。JDDC
   无 MCTS 树，标签来源变为"终局成败回推"或"重跑搜索"，换实现不动下游。
2. **特征提取解耦**：现有特征依赖内部标注（SOP 节点/情绪/满意度），JDDC
   原始数据没有，需映射层（可能 Qwen 打标）。**拿到 5~10 条 JDDC 样例
   就先试跑映射**，别等全量到手才发现对不上。
3. **评估协议定死**（第四节），论文可画"数据量 60→N 增益曲线"，scaling
   趋势本身是有说服力的结果。
4. **模型容量配置化**（1.4）。

### 算力预算测算（Step 2.5 顺带产出）

真正吃算力的不是 PRM 训练（小 MLP），是 **Qwen rollout 生成标签**。
Step 2.5 记录：平均每条轨迹多少次 Qwen 调用、多少秒 → 乘 JDDC 规模 =
总预算。若不可行，二阶段标签策略调整为"部分轨迹 rollout 标注 + 其余
终局成败便宜标签，混合训练"。

---

## 六、Step 1/2 结果（2026-07-15，留出 12 条）

| 模式 | 终点符合率 | 平均轮数 | 严格合规率 | 完成率 |
|------|-----------|---------|-----------|--------|
| terminal（对照，取自 mcts_local_60 同 12 条） | 66.7% | 8.42 | 100% | 100% |
| dense（Step 1 手写步奖励） | 66.7% | **7.67** | 100% | 100% |
| pbrs（Step 2 手写 Φ 塑形） | 66.7% | 8.50 | 100% | 100% |

- 单元验证全通过：回报只回传一次、尺度比 0.63<1、telescoping 塑形和 = −Φ(S0)
- 三种模式终点符合率完全一致；dense 平均快 0.75 轮（唯一可见差异）
- 判据落在"中间地带"，但根因已定位（见下），**不是稠密信号机制失效**

### 关键发现：奖励错配（reward misspecification）

全量 60 条按期望终点拆分：期望 resolution 的 **50/50 全对**，期望
escalation 的 **0/8 全错**（demand_excessive 用户全被硬关单到 S10）。

轨迹核查（refund_14）：用户全程愤怒、坚持超额索赔、威胁投诉——模拟器
行为正确；是 agent 在用户从未接受的情况下 confirm_resolution 关单。
数字验证：S10+angry（10轮）= 1.0−0.3−0.5 = **+0.20** > S11+angry（5轮）
= 0.4−0.3−0.25 = **−0.15**。当前奖励下"带愤怒用户硬关单"恒优于转人工，
MCTS 在正确地优化一个错误的奖励。

**推论**：
1. 终点符合率被结构性封顶在 86.2%（50/58），dense/PBRS 均无法修复
   （塑形不改变轨迹排序，正是 telescoping 的设计保证）
2. 对论文是正面材料：合规率 100% + 双口径评估捕获了"虚假解决"
3. **Step 2.5（树统计落盘）必须等奖励修复后再跑**——旧奖励下的 Q 值
   作 PRM 训练标签会把错配蒸馏进模型

### 修复选项（待裁决）

- **A. 奖励条件化**（推荐）：S10 且用户 angry 时 completion 打折
  （如 ×0.3 → 0.3 < S11 的 0.4）。只改 reward.py；基线 agent 不用
  奖励函数，**基线结果无需重跑**，只重跑 MCTS。
- **B. SOP 关单守卫**：confirm_resolution 要求用户非 angry，否则只有
  comfort/revise/escalate 合法。更符合"SOP 编码业务规则"的论文叙事，
  但改了合规判定 → **基线 60 条也要重跑**（¥30+），且历史数字全部失效。
- **C. A+B 都做**（作最终形态，分两步验证）。
- **D. 不修，作为 finding 写进论文讨论**。终点符合率停在 86%。

## 七、文件改动清单

| 文件 | 改动 |
|------|------|
| `src/mcts.py` | 树统计导出（全树 N≥阈值节点的 (state, Q) 对） |
| `src/prm_data.py` | 新建：`trajectory → List[(features, label)]` 解耦接口 |
| `src/prm_model.py` | 新建：小 MLP/逻辑回归，容量配置化，轨迹级交叉验证 |
| `src/reward.py` | PBRS 塑形项（路线 B）+ 硬编码 Φ 表（Step 2） |
| `src/mcts_env.py` | 稠密奖励开关（Step 1）+ PRM 叶评估开关（路线 A） |
| `src/runner.py` | `--reward-mode {terminal,dense,pbrs,prm}` `--leaf-eval {rollout,prm}` `--budget-mode {sims,wallclock}` |
| `scripts/prm_label_cost.py` | 新建：标签生成算力测算（Step 2.5 内置） |
| `scripts/prm_eval.py` | 新建：留出集评估 + bootstrap CI 固化脚本 |
