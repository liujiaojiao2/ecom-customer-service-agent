# 阶段3/4 组合方案：消融实验 + 论文

> 目前已拿到的核心 claim 数字（阶段0/1/2）：
> - SOP 合规率：基线 25.1% → MCTS **100.0%**（+70.8pp）
> - 任务完成率 100% = 100%，平均对话轮数 6.25 vs 6.00
> - 多模态感知准确率：**95.0%** issue_type，**100%** has_evidence
> - 多模态端到端：return_02 感知→MCTS 9 轮 S10 零违规

**这些数字已经足以支撑一篇论文。** 阶段3/4 的选择是"要在这个基础上加多少边"。

---

## 一、三条路径对比

### 路径 A：**精简收官**（推荐 · 1~2 周）

只做阶段4 里最关键的消融，跳过阶段3 的策略/价值网络（作为 future work 提及）。

**要做的实验**：
1. **全量 60 条 MCTS 评估**：把阶段1 的 12 条扩到 60 条（约 ¥30，2~3 小时）
2. **消融①：有 SOP mask vs 无 mask**：MCTS 内部禁用 legal_actions 过滤（放开非法动作），看合规率崩到多少
3. **消融②：奖励权重敏感性**：3 个权重档位（w_turn ∈ {0.02, 0.05, 0.15}）对比核心指标
4. **消融③：单模态 vs 多模态感知**：文本 only（只喂 first_utterance）vs 图+文+行为 → 分类准确率下降多少
5. **补充分析**：错例分析（哪类场景 MCTS 会失败）、平均搜索时间

**产出**：完整的实验 5 表 + 12 条对话对比样例（可放论文附录）
**风险**：所有 claim 都有数字支撑，但没有"策略/价值网络增益"这个亮点

### 路径 B：**加简版策略网络**（3~4 周）

在路径 A 基础上加**极简策略网络**（不用价值网络）：
- 训练数据：用现有 MCTS 12+ 条对话轨迹做行为克隆（数据合成时间成本大幅缩减）
- 网络架构：MLP 输入 = SOP 节点 one-hot + 用户情绪 one-hot + issue_type one-hot，输出 = 动作分布
- 用途：作为 UCT 先验（U + P·√(N)/n 的 P 项，退化到简化版 PUCT）
- 增益预期：不确定，可能只是让搜索预算从 N=16 降到 N=8 也能达到同样准确率

**产出**：路径 A + "PUCT vs UCT" ablation
**风险**：训练数据太少（12 条对话 ≈ 100 步）可能训不出有效网络；如果做完发现增益不显著，反而弱化论文

### 路径 C：**完整阶段3+4**（6~8 周）

按原计划做数据合成（5000~10000 条）+ 策略/价值网络训练。

**风险最大**：数据合成成本高（可能 ¥100~500）、训练调参耗时、M3 本地跑不动可能需要云 GPU、结果不确定。**秋招期间不建议**。

---

## 二、路径 A 的模块拆解（推荐）

### 实验1：全量 60 条 MCTS（复用阶段1 代码）
- `python src/runner.py --agent mcts --workers 6 --out runs/mcts_60`
- `python src/evaluate.py runs/mcts_60`
- **需求**：MCTS 60 条 vs 基线 60 条同 case 对比表；成本约 ¥30，2~3 小时

### 消融①：SOP mask on/off
- `mcts_env.py` 增加 `mask_enabled` 开关：off 时 `legal_actions` 返回全部 15 个动作，SOP 引擎 step 用 `resync=True` 允许违规继续
- 新脚本 `scripts/ablation_mask.py`：mask=off 跑 12 条，与阶段1 同 12 条对比
- **预期**：合规率从 100% → 30~50%（近似基线），完成率下降

### 消融②：奖励权重敏感性
- `reward.py` RewardConfig 已经支持配置
- 3 组权重配置 × 12 条 = 36 次对话
- **预期**：核心指标（合规率、完成率）不敏感，平均轮数敏感

### 消融③：单模态 vs 多模态感知
- 现有 `perception.py`：多模态版本
- 新增 `perception_text_only.py`：只用文本，不传图片（同一 prompt 模板去掉图）
- 20 条样本各跑一次
- **预期**：文本 only 准确率 80~85%，多模态 95%，差值 10pp

### 论文骨架
- **贡献点1** 数据范式：合成 60 条测试集（阶段0）+ 逆向画像模板（阶段3合成的替代品，作 future work）
- **贡献点2** 多模态感知：Qwen-VL zero-shot 95% 准确率，消融③支撑
- **贡献点3** SOP 约束 MCTS：合规率 25%→100%，消融① 支撑必要性
- **实验章节**：全 60 条主表 + 3 个消融表 + 错例分析
- **限制与 future work**：策略/价值网络训练、用户模拟器保真度实验、真实 JDDC 数据评估

---

## 三、成本与时间估算（路径 A）

| 项 | 时间 | ¥ |
|----|------|---|
| 全量 60 条 MCTS | 2~3 小时 | ~30 |
| 消融① mask off | 1 小时 | ~5 |
| 消融② 权重 ×3 | 3 小时 | ~15 |
| 消融③ 文本 only | 30 分钟 | ~2 |
| 错例分析 + 表格生成 | 半天 | 0 |
| 论文初稿写作 | 1 周 | 0 |
| **合计** | **约 1.5 周** | **~50** |
